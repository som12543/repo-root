# app1 — COBOL Application

This folder contains the source code, build scripts, and tests 
for the COBOL application running through the Enterprise CI pipeline.

- `build.sh` → compilation
- `test.sh` → unit tests
- `src/` → COBOL, JCL, copybooks
